streamlit
langchain
langchain-community
langchain-core
openai
huggingface-hub
requests
python-dotenv
pdfplumber
openpyxl
python-pptx


# RAG Pipeline - Requirements
# Install all: pip install -r requirements.txt

# ── Core ──────────────────────────────────────
pypdf>=4.0.0                    # PDF loading
python-docx>=1.1.0              # DOCX loading
pandas>=2.0.0                   # CSV loading
beautifulsoup4>=4.12.0          # HTML/URL loading
requests>=2.31.0                # URL fetching
lxml>=5.0.0                     # Faster BS4 parser

# ── Chunking ──────────────────────────────────
tiktoken>=0.6.0                 # Token-based chunking (OpenAI tokenizer)

# ── Semantic Chunking (optional) ──────────────
sentence-transformers>=3.0.0    # Semantic chunking strategy
numpy>=1.26.0

# ── Vector Databases (pick one) ───────────────
chromadb>=0.5.0          

# Environment & Configuration Management
python-dotenv>=1.0.1

# LangChain & Open-Source LLM Integration
langchain-huggingface>=0.0.3
langchain-core>=0.2.0
huggingface_hub>=0.23.0

# Document Parsing & Preprocessing
pypdf>=4.2.0
pandas>=2.0.0

# Mathematical & Vector Database Utilities (Adjust based on your chosen VDB client, e.g., chromadb, qdrant-client, or faiss)
numpy>=1.24.0
rank-bm25>=0.2.2

